# 대기열 입장 API 재호출 처리 전략 - ZADDNX vs ZADD

## 배경

`QueueService.enter()`는 한 userId에 대해 여러 번 호출될 수 있다. 현재 구현(`RedisUtil.zAdd` → `opsForZSet().add()`)은 항상 score를 덮어쓰는 순수 `ZADD`이며, `JOIN` 타입에 한해 `enter()` 진입 전 `alreadyActive || alreadyWaiting` 체크로 재입장을 애플리케이션 레벨에서 막고 있다. 이 체크와 실제 `zAdd` 호출 사이에는 원자성이 없다(TOCTOU).

## 비교표

| 구분 | ZADDNX (기존 순번 유지) | ZADD (맨 뒤로 재정렬) |
|---|---|---|
| 동작 방식 | 멤버가 이미 존재하면 score를 갱신하지 않고 무시 | 이미 존재해도 새 score로 덮어써 순번을 재계산 |
| 공정성 | 최초 입장 시점 기준 순번 고정 → 공정 | 마지막 호출 시점이 반영되어 재호출할수록 순번이 뒤로 밀림 |
| 새로고침/네트워크 재시도 UX | 안전. 사용자가 실수로 다시 호출해도 순번 손실 없음 | 불리. 타임아웃 후 클라이언트 자동 재시도만으로도 순번이 초기화될 수 있음 |
| 어뷰징(새치기 재시도) 방지 | 약함. 반복 호출 자체를 막지는 못하고, 다만 순번 조작은 불가 | 강함처럼 보이나, 실제로는 순번이 뒤로 밀리는 것은 "본인 불이익"일 뿐 타인 새치기 방지 효과는 없음 |
| 동시성(레이스) 안전성 | Redis가 원자적으로 처리 → TOCTOU 창을 닫아줌 | 동일하게 원자적이지만, 마지막 쓰기가 이기므로 동시 요청 시 순번이 요청 도착 순서에 따라 튈 수 있음 |
| 서버 부하(트래픽) 감소 효과 | 없음. 호출 횟수 자체는 그대로 서버에 도달 | 없음. 마찬가지로 호출 횟수는 줄지 않음 |
| 이 프로젝트 적합성 | `JOIN`의 애플리케이션 레벨 가드(`alreadyWaiting`)와 의도가 일치 → 가드를 원자적으로 보강하는 역할 | `REJOIN`처럼 "명시적으로 다시 줄 서겠다"는 의도가 있을 때만 적합 |

## ZADD + 공지 방식으로 트래픽을 보호할 수 있는가?

아니오. 정확히는 "직접적으로는 아니다"에 가깝다.

- `ZADD`는 재호출한 유저 본인의 순번을 뒤로 보낼 뿐, HTTP 요청이 서버(Redis)에 도달하는 것 자체를 막지 못한다. 즉 CPU·네트워크·Redis 커맨드 처리량 관점의 "트래픽"은 줄어들지 않는다.
- 이 프로젝트에서 실제로 트래픽을 보호하는 장치는 이미 따로 존재한다.
  - `enter()` : `JOIN` 타입일 때 `alreadyActive || alreadyWaiting`을 먼저 검사해 재입장 자체(추가 `INCR` + `ZADD`)를 원천 차단한다.
  - `status()` : `nextPollIntervalMillis(rank)`로 순번에 따라 폴링 주기를 1초~30초까지 차등화해 뒤쪽 유저의 폴링 부하를 억제한다.
- "공지"로 트래픽을 실제로 줄이려면 클라이언트가 상태를 알기 위해 반복 폴링(GET `/status`)하는 구조를 SSE/WebSocket 푸시로 바꿔 폴링 요청 자체를 없애는 방향이 맞다. `ZADD`/`ZADDNX` 중 무엇을 쓰는지는 이 트래픽 감소와 무관하다.
- 다만 `ZADD`가 주는 부수 효과는 있다. 사용자가 "새로고침하면 순번이 밀린다"는 걸 학습하면 자발적으로 재호출을 자제하게 되는 간접적·심리적 유인은 된다. 그러나 이는 부작용(정상적인 네트워크 재시도까지 함께 페널티) 없이는 얻기 어려운 효과다.

## 결론 및 권장

- `JOIN` 경로: 의도가 "이미 대기 중이면 그대로 둔다"이므로 `ZADDNX`가 의도와 정확히 일치하며, 현재의 체크-후-쓰기(TOCTOU) 구조를 원자적으로 보강할 수 있다. 별도 분산 락 없이 레이스 컨디션을 닫는 효과가 있다.
- `REJOIN` 경로: 이미 `remove` 후 재등록하는 명시적 로직이 있으므로 `ZADD` 그대로 유지하는 것이 맞다.
- 트래픽(서버 부하) 보호는 Redis 커맨드 선택(`ZADD` vs `ZADDNX`)이 아니라, 애플리케이션 레벨 재입장 가드와 폴링 주기 차등화(이미 구현됨), 그리고 필요하다면 폴링 대신 푸시로의 전환에서 나온다.
